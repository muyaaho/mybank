---
apiVersion: v1
kind: ConfigMap
metadata:
  name: pfm-core-service-config
  namespace: mybank
data:
  application.yml: |
    server:
      port: 8082
    spring:
      application:
        name: pfm-core-service
      data:
        mongodb:
          host: mongodb
          port: 27017
          database: mybank_pfm
          username: root
          password: rootpassword
          authentication-database: admin
        redis:
          host: redis
          port: 6379
      kafka:
        bootstrap-servers: kafka:9093
        consumer:
          group-id: pfm-core-service
          auto-offset-reset: earliest
          enable-auto-commit: false
      cache:
        type: redis
        redis:
          time-to-live: 1800000
    eureka:
      client:
        service-url:
          defaultZone: http://service-discovery:8761/eureka/
        register-with-eureka: true
        fetch-registry: true
      instance:
        prefer-ip-address: true
    management:
      endpoints:
        web:
          exposure:
            include: health,info,metrics,prometheus

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pfm-core-service
  namespace: mybank
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pfm-core-service
  template:
    metadata:
      labels:
        app: pfm-core-service
    spec:
      containers:
        - name: pfm-core-service
          image: mybank/pfm-core-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8082
          env:
            - name: SPRING_CONFIG_LOCATION
              value: "file:/config/application.yml"
          volumeMounts:
            - name: config
              mountPath: /config
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /pfm/health
              port: 8082
            initialDelaySeconds: 120
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /pfm/health
              port: 8082
            initialDelaySeconds: 90
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: pfm-core-service-config

---
apiVersion: v1
kind: Service
metadata:
  name: pfm-core-service
  namespace: mybank
spec:
  type: ClusterIP
  selector:
    app: pfm-core-service
  ports:
    - port: 8082
      targetPort: 8082
