---
apiVersion: v1
kind: ConfigMap
metadata:
  name: investment-service-config
  namespace: mybank
data:
  application.yml: |
    server:
      port: 8084
    spring:
      application:
        name: investment-service
      data:
        mongodb:
          host: mongodb
          port: 27017
          database: mybank_investment
          username: root
          password: rootpassword
          authentication-database: admin
      kafka:
        bootstrap-servers: kafka:9093
        consumer:
          group-id: investment-service
          auto-offset-reset: earliest
          enable-auto-commit: false
    eureka:
      client:
        service-url:
          defaultZone: http://service-discovery:8761/eureka/
        register-with-eureka: true
        fetch-registry: true
      instance:
        prefer-ip-address: true
    management:
      endpoints:
        web:
          exposure:
            include: health,info,metrics,prometheus

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: investment-service
  namespace: mybank
spec:
  replicas: 2
  selector:
    matchLabels:
      app: investment-service
  template:
    metadata:
      labels:
        app: investment-service
    spec:
      containers:
        - name: investment-service
          image: mybank/investment-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8084
          env:
            - name: SPRING_CONFIG_LOCATION
              value: "file:/config/application.yml"
          volumeMounts:
            - name: config
              mountPath: /config
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /invest/health
              port: 8084
            initialDelaySeconds: 120
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /invest/health
              port: 8084
            initialDelaySeconds: 90
            periodSeconds: 5
      volumes:
        - name: config
          configMap:
            name: investment-service-config

---
apiVersion: v1
kind: Service
metadata:
  name: investment-service
  namespace: mybank
spec:
  type: ClusterIP
  selector:
    app: investment-service
  ports:
    - port: 8084
      targetPort: 8084
